VITE_API_BASE_URL=https://pcapi.selecteg.com

# ---- E2E (Playwright) overrides ----
# Target app + API; defaults to the deployed SELECT Petty Cash environment.
E2E_BASE_URL=https://pc.selecteg.com
E2E_API_BASE_URL=https://pcapi.selecteg.com

# Role credentials (defaults are the documented test accounts).
E2E_ADMIN_EMAIL=farida@company.com
E2E_ADMIN_PASSWORD=555555
E2E_MANAGER_EMAIL=youssef@company.com
E2E_MANAGER_PASSWORD=555555
E2E_FINANCE_EMAIL=finance@company.com
E2E_FINANCE_PASSWORD=555555
E2E_EMPLOYEE_EMAIL=menna@company.com
E2E_EMPLOYEE_PASSWORD=555555

# Set to 1 to run the data-creating scenario specs:
#   submit/approve/pay/reject requests, create+delete users, create departments
#   (persists — no delete API), direct grants (+balance, permanent), mark
#   notifications read, change+restore passwords (restored in a finally block).
# Off by default: the main suite stays read-only.
# E2E_MUTATIONS=1
